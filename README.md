# my_app_cat

my-app-cat

## Getting Started

# 执行flutter pub run build_runner watch自动构建model的Json序列化话反序列化代码
# 执行flutter pub run build_runner build手动构建model的Json序列化话反序列化代码

# keytool -genkey -v -keystore amap_map_test.jks -keyalg RSA -keysize 2048 -validity 10000 -alias amap_map_test